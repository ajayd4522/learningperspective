# learningperspective
adding some features for future proof references
git command lines
git push origin main
git commit -m message
git status
git log
git commit -v
git pull 
*git branch is very important to understand because you can only commit pull request one time with same branch name so if you have make another pull requestshould another branch name."

